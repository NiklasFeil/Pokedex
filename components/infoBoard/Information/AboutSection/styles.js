import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        backgroundColor: "red",
    },
    entryText: {
        fontSize: 12,
    },
});

export default styles;